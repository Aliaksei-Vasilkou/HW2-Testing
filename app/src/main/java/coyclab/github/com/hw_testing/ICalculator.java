package coyclab.github.com.hw_testing;

interface ICalculator {
    String addition(int x, int y);
    String subtraction(int x, int y);
    String multiplication(int x, int y);
    String division(int x, int y);
}
